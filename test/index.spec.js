import chai from 'chai';

chai.expect();

const expect = chai.expect;

let lib;

describe('Nothing', () => {
  before(() => {
  });
  describe('when I need the name', () => {
    it('should return the name', () => {
    });
  });
});
